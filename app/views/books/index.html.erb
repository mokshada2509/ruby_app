<h1 class = "current-category"><%= params[:category] %></h1>

<% if @books.count == 0 %>
  <h1>There are no books in this category </h1>
<% else %>
  <% @books.each do |book1| %>
    <h2><%= link_to book1.title, book_path(book1) %></h2>
  <% end %>
<% end %>